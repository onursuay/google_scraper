{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "playwright install chromium --with-deps && python dashboard.py",
    "restartPolicyType": "ON_FAILURE"
  }
}
